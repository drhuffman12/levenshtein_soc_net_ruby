require "spec_helper"

describe LevenshteinSocNetRuby do
  it "has a version number" do
    expect(LevenshteinSocNetRuby::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
